<h1>Here are a list of your study words</h1>
<% if @vocab.any? %>
  <ul>
    <% @vocab.each do |word| %>
      <li><%= vocab.new_word %>,<%= vocab.new_defintion %></li>
    <% end %>
  </ul>
<% else %>
  <p>There are currently no records to display.</p>
<% end %>
